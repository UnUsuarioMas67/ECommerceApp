@model ECommerce.Dashboard.Models.Auth.LoginViewModel
@{
    ViewData["Title"] = "Login";
}

<form asp-action="Login" method="post">
    <div class="mb-3">
        <label asp-for="LoginRequest.Email" class="form-label">Email address</label>
        <input type="email" class="form-control" asp-for="LoginRequest.Email">
        <span class="small text-danger" asp-validation-for="LoginRequest.Email"></span>
    </div>
    <div class="mb-3">
        <label asp-for="LoginRequest.Password" class="form-label">Password</label>
        <input type="password" class="form-control" asp-for="LoginRequest.Password">
        <span class="small text-danger" asp-validation-for="LoginRequest.Password"></span>
    </div>
   
    <button type="submit" class="btn btn-primary">Submit</button>
</form>

